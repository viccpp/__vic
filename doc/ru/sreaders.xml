<chapter xml:id="sreaders">
<title><tt>__vic/sreaders/</tt></title>


<chapter xml:id="sreader">
<title><tt>sreader</tt> concept</title>

<code-block lang="C++"><![CDATA[
template<class T>
class ]]><nt>sreader</nt><![CDATA[
{
public:
    sreader(sreader &&o); or sreader(const sreader &o);
    sreader_result<T> operator()(); // throws on errors
};
]]></code-block>

<p>Concept, используемый алгоритмами библиотеки для поэлементного чтения
логической последовательности элементов типа <tt>T</tt>. Является
обобщением и переработкой concept'а <tt>InputIterator</tt>. В частности,
он лучше обрабатывает потоки ввода/вывода, строки с нулевым терминатором и
другие последовательности, в которых <tt>end</tt>-итератор не имеет смысла или
его получение дорого. В то же время, традиционные пары итераторов
[<tt>begin</tt>,<tt>end</tt>) являются просто частным случаем и полностью
поддерживаются адаптером <tt>__vic::iterator_sreader</tt>.</p>

<p>"sreader" произносится как "<b>S</b>-reader", где <b>S</b> означает
"Sequential", "Sequence" или "Stream".</p>

<p>В случаях, когда конкретный класс удовлетворяет требованиям данного concept
для некоторого <tt>T</tt>, говорят, что он <b>моделирует</b> concept
<tt>sreader&lt;T></tt>.</p>

<p>Каждый объект sreader должен быть move- или copy-constructible.</p>

<section><title>Члены класса</title>

<synopsis>
<prototype>sreader_result&lt;T> operator()()</prototype>
<p>Пытается получить следующий элемент последовательности. В случе успеха,
возвращённый объект - <tt>true</tt> и его функция-член <tt>value()</tt>
возвращает прочитанный элемент. Если больше элементов не осталось (EOF),
возвращает <tt>false</tt>. Бросает исключения в остальных случаях.</p>
</synopsis>

</section>

</chapter>


<chapter xml:id="sreader_result">
<title><tt>sreader_result</tt> concept</title>

<code-block lang="C++"><![CDATA[
template<class T>
interface ]]><nt>sreader_result</nt><![CDATA[
{
    explicit operator bool() const;
    T value() const;
};
]]></code-block>

<p>Результат чтения из sreader. Содержит статус операции чтения
(успешно/неуспешно) и прочитанное значение, если чтение было успешным.
Объекты sreader result (тривиально) копируемы и присваиваемы.</p>

<p>Реализации типов sreader result обеспечивают поддержку structured bindings
в следующей форме:</p>
<code-block lang="C++">
auto [value, ok] = reader();
</code-block>

<p>Так как в C++98 отсутсвует возможность использования спецификатора типа
<tt>auto</tt>, использование разных типов для результатов непрактично. В режиме
данного стандарта sreader'ы возвращают тип <xref to="sread_result"/> (или тип,
неявно преобразуемый к нему).</p>

<section><title>Члены класса</title>

<synopsis>
<prototype>explicit operator bool() const</prototype>
<p>Возвращает <tt>true</tt>, если чтение было успешным и <tt>value()</tt>
содержит прочитанное значение. <tt>false</tt>, обычно, сигнализирует о конце
файла.</p>
</synopsis>

<synopsis>
<prototype>T value() const</prototype>
<p>Возвращает прочитанное значение.</p>
<precondition><tt>*this == true</tt></precondition>
</synopsis>

</section>

<section><title>Примеры</title>
<code-block lang="C++">
// Basic usage (C++11+)
while(auto res = reader())
{
    // Use res.value() ...
}
</code-block>
<code-block lang="C++">
// Using structured bindings (C++17+)
for(;;)
{
    auto [value, ok] = reader();
    if(!ok) break;
    // Use value ...
}
</code-block>
<code-block lang="C++">
// C++26+
while(auto [value, _] = reader())
{
    // Use value ...
}
</code-block>
<code-block lang="C++">
// Legacy C++98
while(__VIC_SREAD_RESULT(T) res = reader())
{
    // Use res.value() of type T ...
}
</code-block>
</section>

</chapter>


<chapter xml:id="sread_result">
<title><tt>sread_result</tt></title>

<code-block lang="C++"><![CDATA[
#include<__vic/sreaders/result.h>

#if __cplusplus >= 201103L // C++11
#define __VIC_SREAD_RESULT(T) auto
#else // C++98
#define __VIC_SREAD_RESULT(T) ::__vic::sread_result<T>
#endif

struct sread_eof_t;
inline constexpr sread_eof_t sread_eof;

template<class T>
class sread_result
{
public:
    sread_result();
    sread_result(sread_eof_t);
    sread_result(T v);

    explicit operator bool() const;
    T value() const;
};
]]></code-block>

<p>Конкретный тип (шаблон), моделирующий <xref to="sreader_result"/>.</p>

<p>Макрос <tt>__VIC_SREAD_RESULT(T)</tt> используется в качестве типа
возвращаемого значения в коде, который должен быть совместим с C++98, вместо
<tt>auto</tt>:</p>
<code-block lang="C++"><![CDATA[
__VIC_SREAD_RESULT(char) result = chars_reader();
]]></code-block>


<section><title>Члены класса</title>

<synopsis>
<prototype>sread_result()</prototype>
<prototype>sread_result(sread_eof_t)</prototype>
<postcondition><tt>!*this</tt></postcondition>
</synopsis>

<synopsis>
<prototype>sread_result(T v)</prototype>
<postcondition><tt>*this &amp;&amp; value() == v</tt></postcondition>
</synopsis>

<synopsis>
<prototype>explicit operator bool() const</prototype>
<p>Returns the read operation status.</p>
</synopsis>

<synopsis>
<prototype>T value() const</prototype>
<p>Returns the value read.</p>
<precondition><tt>*this == true</tt></precondition>
</synopsis>

</section>

</chapter>


<chapter xml:id="sread_byte_result">
<title><tt>sread_byte_result</tt></title>

<code-block lang="C++"><![CDATA[
#include<__vic/sreaders/result.h>

#if __cplusplus >= 201103L // C++11
#define __VIC_SREAD_BYTE_RESULT auto
#else // C++98
#define __VIC_SREAD_BYTE_RESULT ::__vic::sread_byte_result
#endif

class sread_byte_result
{
public:
    sread_byte_result();
    sread_byte_result(sread_eof_t);

    sread_byte_result(unsigned char v);
    sread_byte_result(sread_result<unsigned char> r);

    sread_byte_result(char v);
    sread_byte_result(sread_result<char> r);

    sread_byte_result(std::byte v);
    sread_byte_result(sread_result<std::byte> r);

    sread_byte_result(char8_t v);
    sread_byte_result(sread_result<char8_t> r);

    unsigned char value() const;
    explicit operator bool() const;
};
]]></code-block>

<p>Результат чтения <tt>unsigned char</tt> из sreader. Совместим с любым
представлением байта, включая:</p>
<list style="bulleted">
    <item><tt>char</tt></item>
    <item><tt>unsigned char</tt></item>
    <item><tt>std::byte</tt></item>
    <item><tt>char8_t</tt></item>
</list>

<section><title>Пример</title>
<code-block lang="C++"><![CDATA[
__vic::sread_result<char> char_reader();

__VIC_SREAD_BYTE_RESULT res = char_reader();
// res.value() is unsigned char
]]></code-block>
</section>

</chapter>


<chapter xml:id="sread_value">
<title><tt>sread_value</tt></title>

<code-block lang="C++"><![CDATA[
#include<__vic/sreaders/result.h>

template<class SReadResult>
struct sread_value
{
    typename type;
};

template<class SReadResult>
using sread_value_t = typename sread_value<SReadResult>::type;
]]></code-block>

<p>Метафункция, возвращающая тип возвращаемый функцией-членом <tt>value()</tt>,
для указанного <tt>SReadResult</tt>.</p>

<section><title>Пример</title>
<code-block lang="C++"><![CDATA[
__vic::sread_value<__vic::sread_result<int> >::type value; // int value;
]]></code-block>
</section>

</chapter>


<chapter xml:id="iterator_sreader">
<title><tt>iterator_sreader</tt></title>

<code-block lang="C++"><![CDATA[
#include<__vic/sreaders/iterator.h>

template<class InputIterator,
         class T = typename std::iterator_traits<InputIterator>::value_type>
class iterator_sreader
{
public:
    iterator_sreader(InputIterator begin, InputIterator end);
    sread_result<T> operator()();
    InputIterator position() const;
};

template<class InputIterator> iterator_sreader<InputIterator>
make_iterator_sreader(InputIterator begin, InputIterator end);

template<class T, class InputIterator> iterator_sreader<InputIterator,T>
make_iterator_sreader_for(InputIterator begin, InputIterator end);
]]></code-block>

<p>Адаптер для традиционной [<tt>begin</tt>,<tt>end</tt>) пары итераторов.</p>

<p>Дополнительная функция <tt>position()</tt> возвращает текущую позицию
итератора внутри диапазона.</p>

<p>Может быть создан с помощью конструктора или одной из функций
<tt>make_...</tt>.</p>

</chapter>


<chapter xml:id="iterator_sreader_n">
<title><tt>iterator_sreader_n</tt></title>

<code-block lang="C++"><![CDATA[
#include<__vic/sreaders/iterator.h>

template<class InputIterator,
         class T = typename std::iterator_traits<InputIterator>::value_type>
class iterator_sreader_n
{
public:
    iterator_sreader_n(InputIterator begin, size_t n);
    sread_result<T> operator()();
    InputIterator position() const;
};

template<class InputIterator> iterator_sreader_n<InputIterator>
make_iterator_sreader_n(InputIterator begin, size_t n);

template<class T, class InputIterator> iterator_sreader_n<InputIterator,T>
make_iterator_sreader_n_for(InputIterator begin, size_t n);
]]></code-block>

<p>Адаптер для итератора со счётчиком элементов.</p>

<p>Дополнительная функция <tt>position()</tt> возвращает текущую позицию
итератора внутри диапазона.</p>

<p>Может быть создан с помощью конструктора или одной из функций
<tt>make_...</tt>.</p>

</chapter>


<chapter xml:id="container_sreader">
<title><tt>container_sreader</tt></title>

<code-block lang="C++"><![CDATA[
#include<__vic/sreaders/container.h>

template<class Cont, class T = typename Cont::value_type>
class container_sreader
{
public:
    explicit container_sreader(const Cont &c);
    sread_result<T> operator()();
    typename Cont::const_iterator position() const;
};

template<class Cont>
container_sreader<Cont> make_container_sreader(const Cont &c);

template<class T, class Cont>
container_sreader<Cont,T> make_container_sreader_for(const Cont &c);
]]></code-block>

<p>Адаптер для контейнеров в стиле STL, имеющих члены <tt>begin()</tt> и
<tt>end()</tt>.</p>

<p>Дополнительная функция <tt>position()</tt> возвращает текущую позицию
итератора внутри диапазона.</p>

<p>Может быть создан с помощью конструктора или одной из функций
<tt>make_...</tt>.</p>

</chapter>


<chapter xml:id="cstring_sreader">
<title><tt>cstring_sreader</tt></title>

<code-block lang="C++"><![CDATA[
#include<__vic/sreaders/cstring.h>

template<class charT>
class basic_cstring_sreader
{
public:
    explicit basic_cstring_sreader(const charT *s);
    sread_result<charT> operator()();
    const charT *position() const;
};
using cstring_sreader = basic_cstring_sreader<char>;

template<class charT>
basic_cstring_sreader<charT> make_cstring_sreader(const charT *s);
]]></code-block>

<p>Адаптер для C-строк с нулевым терминатором.</p>

<p>Дополнительная функция <tt>position()</tt> возвращает текущую позицию
указателя внутри строки.</p>

<p>Может быть создан с помощью конструктора или функции <tt>make_...</tt>.</p>

</chapter>


<chapter xml:id="string_sreader">
<title><tt>string_sreader</tt></title>

<code-block lang="C++"><![CDATA[
#include<__vic/sreaders/string.h>

template<class charT>
class basic_string_sreader
{
public:
    template<class Tr, class Al>
    explicit basic_string_sreader(const std::basic_string<charT,Tr,Al> &s);
    sread_result<charT> operator()();
    const charT *position() const;
};
using string_sreader = basic_string_sreader<char>;

template<class charT, class Tr, class Al>
basic_string_sreader<charT>
    make_string_sreader(const std::basic_string<charT,Tr,Al> &s);
]]></code-block>

<p>Адаптер для <tt>std::basic_string</tt>.</p>

<p>Дополнительная функция <tt>position()</tt> возвращает текущую позицию
указателя внутри строки.</p>

<p>Может быть создан с помощью конструктора или функции <tt>make_...</tt>.</p>

</chapter>


<chapter xml:id="cstream_sreader">
<title><tt>cstream_sreader</tt></title>

<code-block lang="C++"><![CDATA[
#include<__vic/sreaders/cstream.h>

class cstream_sreader
{
public:
    explicit cstream_sreader(std::FILE *fp);
    sread_result<char> operator()() { return __vic::read(fp); }
};

cstream_sreader make_cstream_sreader(std::FILE *fp);
]]></code-block>

<p>Моделирует <tt>sreader&lt;char></tt> для <tt>std::FILE</tt>.</p>

<p>Может быть создан с помощью конструктора или функции <tt>make_...</tt>.</p>

</chapter>


</chapter>
